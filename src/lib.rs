mod network {
    fn connect() {

    }
}

// Call (not here because it's a library) with network::connect()

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
